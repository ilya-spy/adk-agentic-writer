services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
    volumes:
      - ./src:/app/src
    restart: unless-stopped
    env_file:
      - .env

